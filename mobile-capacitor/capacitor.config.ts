import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nuist.mobile',
  appName: 'Nuist Mobile',
  webDir: '../public', // load Laravel public folder from the project root
};

export default config;
